{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile --filter @mularpay/mularpay-api && pnpm --filter @mularpay/mularpay-api prisma:generate && pnpm --filter @mularpay/mularpay-api build"
  },
  "deploy": {
    "startCommand": "node dist/src/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
